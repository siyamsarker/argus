# ============================================================================
# Argus — Loki & Grafana Health Monitor
# Copy this file to .env and fill in the values:
#
#   cp .env.example .env
#
# SECURITY: The .env file contains secrets (webhook URL). The install script
# sets it to chmod 600. Never commit .env to version control.
# ============================================================================

# ---------------------------------------------------------------------------
# Required — Argus will refuse to start without these
# ---------------------------------------------------------------------------

# Base URL(s) of the Loki instance(s) (without trailing /ready).
# For a single instance:
#   LOKI_URL=http://192.168.1.50:3100
# For multiple instances (comma-separated):
#   LOKI_URL=http://192.168.1.50:3100,http://192.168.1.51:3100
LOKI_URL=http://your-loki-host:3100

# Base URL(s) of the Grafana instance(s) (without trailing /api/health).
# For a single instance:
#   GRAFANA_URL=http://192.168.1.50:3000
# For multiple instances (comma-separated):
#   GRAFANA_URL=http://192.168.1.50:3000,http://192.168.1.51:3000
GRAFANA_URL=http://your-grafana-host:3000

# Discord webhook URL for alert notifications
# Create one at: Discord Server → Settings → Integrations → Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ---------------------------------------------------------------------------
# Optional — sensible defaults are used when omitted
# ---------------------------------------------------------------------------

# How often to poll health endpoints (in seconds). Default: 120 (2 minutes)
CHECK_INTERVAL_SECONDS=120

# Number of consecutive failures before declaring a service unhealthy. Default: 2
FAILURE_THRESHOLD=2

# HTTP request timeout for health checks (in seconds). Default: 10
REQUEST_TIMEOUT_SECONDS=10

# Logging verbosity: DEBUG, INFO, WARNING, ERROR, CRITICAL. Default: INFO
LOG_LEVEL=INFO
